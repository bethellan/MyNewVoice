MyNewVoice v123 - Settings / Content Editor real-screen rebuild

Purpose:
- Stop the repeated cropping of Content Editor footer/buttons by removing the floating-window behaviour.

Changes:
- Content Editor is moved to document.body when opened and fills the viewport as its own screen.
- The main app container is hidden while the Content Editor screen is open.
- Section and phrase editors keep one-entry-per-row table layout.
- Tables scroll vertically and horizontally inside the table area.
- Back/Save footer sits outside the table scroll area and remains visible.

Preserved:
- Dad-facing communication screen unchanged.
- Backup/export/import schema unchanged.
- Existing top-level backup fields unchanged.
- Phrase/category/media IDs unchanged.
- Local images and recorded voices unchanged.
- Speech fallback behaviour unchanged.
