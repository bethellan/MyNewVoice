MyNewVoice v128 - Picture options async audit fix

Scope:
- Fixes delayed Content Editor picture options appearing only after leaving Settings/Content Editor.
- Removes duplicate manual picture-trigger binding.
- Uses one delegated click path from the picture table cell.
- Opens the picture options panel immediately, before IndexedDB media lookup completes.
- Loads saved-picture status asynchronously after the panel is visible.
- Cancels stale pending picture-option lookups when leaving Settings or Content Editor.
- Keeps section and phrase table layout: one entry per row, horizontal table scroll if needed.

Preserved:
- Dad-facing communication screens.
- Speech behaviour and local audio fallback.
- Local image/audio storage keys.
- Backup/export/import schema and mediaRecords.
- Category IDs, phrase IDs and media IDs.
