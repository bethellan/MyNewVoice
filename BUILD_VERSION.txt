v216_settings_dead_handler_cleanup

Removed stale Settings event handlers and control-sync code for Settings controls that are no longer rendered.
Kept stored settings fields for backup compatibility while reducing patch-drift in the visible Settings flow.
Includes v215 visible Update App button.

v215_visible_update_app_button

Restored a visible Update App button inside Offline Use using the existing safe app refresh/update path.
Updated Help troubleshooting wording so GitHub/iPhone/iPad updates have a clear in-app route: Update App, then Save Offline, then test.
Includes v214 visible version metadata alignment.

v214_align_visible_version_metadata

Aligned the app-facing version shown in Information, app-version.json and runtime exports after the v213 Git housekeeping checkpoint.
Keeps v213 as the Git-only cleanup that ignored the iCloud-created app-version 2.json placeholder.
Includes v212 release-candidate wording tidy.

v212_release_candidate_wording_tidy

Updated release-candidate Help/About fallback wording so visible guidance matches the current Settings flow.
Refreshed the hard-coded About version fallback to v212 before runtime version replacement.
Includes v211 standardised Settings action buttons and cleanup.

v211_standardised_settings_action_buttons

Standardised spacing, height and wrapping for Settings, modal and editor action buttons such as Cancel, OK, Confirm, Back and Return.
Kept existing button colours and behaviours while making the action rows follow one consistent spacing rhythm.
Includes v210 tighter Yes / No quick-button spacing.

v210_tighter_quick_yes_no_spacing

Reduced the vertical margin and padding around the optional Yes / No quick-button strip.
Kept the buttons comfortably tappable while giving more space back to the communication area, with a tighter mobile spacing rule.
Includes v209 unreachable speech-off cleanup.

v209_unreachable_speech_off_cleanup

Removed unreachable speech-off branches now that generated/recorded speech is kept internally enabled.
Edit mode no longer shows an extra toast when toggled; the switch state is the feedback.
Te Reo mode keeps the fluent-speaker warning when enabled and uses a shorter standard toast when turned off.
Includes v208 speech always-on and compression warning update.

v208_speech_always_on_and_compression_warning

Removed the visible Spoken voice on/off setting and old Settings references to visual-popup-only speech mode.
Speech is now kept internally on, including when older backups/settings contain speechEnabled off.
Strengthened image compression warnings to recommend exporting a complete backup first because quality loss cannot be undone without restoring a backup.
Includes v207 Classic theme CSS consolidation.

v207_consolidate_classic_theme_css

Merged the later Classic theme variable overrides into the first body[data-theme="default"] owner.
Removed the redundant later Classic body blocks while preserving the final visible Classic colours.
Includes v206 unused category theme lookup cleanup.

v206_remove_unused_category_theme_lookup

Removed the empty category theme override lookup and its now-unused active-theme helper.
Category colours now come directly from the baseline category metadata plus user category settings.
Includes v205 fixed theme setting cleanup.

v205_remove_fixed_theme_setting

Removed the visible Classic-only Theme row from Settings along with its unreachable change/update-control code.
Kept internal theme normalization and legacy aliases so older backups/settings still load safely as Classic.
Includes v204 dead theme-toggle CSS cleanup.

v204_dead_theme_toggle_css_cleanup

Removed dead .theme-toggle CSS and references from shared selector lists after confirming no app markup renders that class.
Preserves the live Classic theme setting compatibility and all app-bar controls.
Includes v203 legacy theme selector cleanup.

v203_legacy_theme_selector_cleanup

Removed older unreachable non-Classic theme CSS selectors for former dark, midnight and concept themes.
Kept Classic/default styling and JavaScript legacy aliases so older saved settings still normalize safely.
Includes v202 unreachable theme override cleanup.

v202_unreachable_theme_override_cleanup

Removed a remaining non-Classic theme-specific override cluster from CSS after v201 made Classic the only active selectable theme.
Preserves legacy theme aliases so old backups/settings still normalize to Classic.
Includes v201 Classic theme payload cleanup.

v201_classic_theme_payload_cleanup

Removed inactive non-Classic theme palette payloads from CSS and JavaScript now that Classic is the only selectable active theme.
Kept legacy theme aliases so older backups/settings still normalize safely to Classic.
Includes v200 submenu phrase row owner.

v200_submenu_phrase_row_owner

Folded the remaining submenu phrase-row text alignment and vertical colour-strip rules into the main submenu row owner, then removed the older v124 hotfix block.
Keeps the same visible submenu rows while reducing late CSS override layers.
Includes v199 submenu fallback icon display owner.

v199_submenu_fallback_icon_display_owner

Moved submenu fallback-icon show/hide ownership into the main submenu media block, removing the later duplicate hotfix while preserving phrase-row image/fallback behaviour.
Includes v198 submenu phrase button border owner.

v198_submenu_phrase_button_border_owner

Consolidated submenu phrase-button border ownership so the final thin border and vertical colour strip are defined by the main submenu button block instead of a later hotfix undoing an earlier thick border.
Preserves the visible submenu/list phrase-row design.
Includes v197 Simple List CSS owner.

v197_simple_list_css_owner

Moved the Simple List heading/gap styling into a scoped List View owner block beside the other view owners, and removed a dangling view selector left behind in the stylesheet.
Preserves the visible List, Menu and Grid layouts.
Includes v196 display-mode state owner.

v196_display_mode_state_owner

Consolidated List/Menu/Grid body-state ownership into one helper so main-menu, submenu and app-bar updates no longer repeat separate display-mode class logic.
Preserves existing List, Menu, Grid, swipe, Quick Yes / No and app-bar layouts.
Includes v195 neutral app-bar touch states.

v195_neutral_app_bar_touch_states

Kept the top app-bar icon buttons visually neutral after touch/focus and cleared sticky iOS focus after app-bar actions, reducing view-button shading and swipe repaint flicker without changing the view/render systems.
Includes v194 app-bar control consistency.

v194_app_bar_control_consistency

Tidied the app-bar control family after the wide switch change: settings now has an explicit button type, app-bar spacing is owned by one gap variable, and very narrow iPhones get tighter control sizing to avoid crowding.
Includes v193 wide app-bar switch pills.

v193_wide_app_bar_switch_pills

Changed the Edit and Te Reo app-bar controls to wider visible pill switches so the visible control and touch target match on iPhone.
Kept pointerdown activation and tightened very-small-screen app-bar spacing so controls remain on the right.
Includes v192 app-bar switch pointerdown activation.

v192_app_bar_switch_pointerdown_activation

Made the Edit and Te Reo app-bar switches activate from the full housed switch area on pointerdown, improving iPhone reliability when a finger drifts slightly before release.
Widened the switch housing while keeping the compact visual pill.
Includes v191 app-bar switch hit-target cleanup.

v191_app_bar_switch_hit_target_cleanup

Cleaned up the Edit and Te Reo app-bar switch hit targets so the whole housed switch button is selected on touch devices.
The visible switch remains compact, while child elements and pseudo-elements no longer compete with the outer button for taps.
Includes v190 About wording update.

v190_about_local_culture_wording

Updated the About MyNewVoice wording to explain that the app grew from Andrew Bethell supporting his father after a stroke, and that it has been adapted to feel local, personal, familiar and respectful of local culture.
Includes v189 Te Reo starter pack completion.

v189_te_reo_default_starter_pack_completion

Completed the built-in Te Reo starter pack for default app phrases outside My People.
Added missing Daily Routine, Social Chat, Activities and Memories starter wording, with pronunciation helper text for each added phrase.
The Settings Te Reo Defaults checker now reports zero missing built-in default phrase entries, excluding My People names by design.
Includes v188 Settings Te Reo defaults check/fill tools and Comfort category key fix.
Includes v187 Settings Help guide and icon actions, v186 Help password note, v185 app-bar Help and touch refactor, v184 Settings Help manual, v183 phone app-bar slot cleanup, v182 reliable app-bar pointer capture, v181 neutral view icons and boot render, v180 app-bar button style isolation, v179 dead Introduction Settings cleanup, v178 remove editor diagnostics, v177 classic theme baseline, v176 mobile editor no-sideways-focus fix, v175 photo memory zoom guard fix, v173 mobile editor focus containment, v172 reliable app-bar controls, v171 Settings entry overlay cleanup, v170 iPhone app-bar tap target fix, v169 Information overlay sequence fix, v168 direct Settings/Information card, v167 quick Yes / No touch colour fix, v166 removed Introduction Message controls, v165 iOS app refresh cache fix, v164 mobile app-bar tap targets, v163 List View render ownership, v160 Edit unlock opens Settings, v159 Settings escape fix and tab CSS cleanup, v158 Settings theme simplification, v157 Content Editor CSS cleanup, v156 mobile Content Editor containment and v155 app-bar icon encoding.
